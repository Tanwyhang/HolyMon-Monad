{
  "name": "@holymon/backend",
  "version": "1.0.0",
  "description": "HolyMon backend service with Bun + ElizaOS integration",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "test:eliza": "bun test-eliza-integration.ts"
  },
  "dependencies": {
    "@elizaos/core": "^1.7.2",
    "@elizaos/plugin-groq": "^1.0.4",
    "viem": "^2.21.54",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.2"
  }
}
