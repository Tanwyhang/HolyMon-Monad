# Environment
MONAD_RPC_URL=https://testnet-rpc.monad.xyz
CHAIN_ID=14314

# Backend Wallet (REQUIRED for production)
PRIVATE_KEY=0x...

# Contract Addresses (will be populated after deployment)
AGENT_REGISTRY_ADDRESS=
TOKEN_LAUNCHPAD_ADDRESS=
MON_STAKING_ADDRESS=

# ElizaOS LLM Configuration
GROQ_API_KEY=gsk_xxx  # Get free key at https://console.groq.com/
ELIZAOS_MODEL_PROVIDER=groq
ELIZAOS_HYBRID_RATIO=0.5  # 0.5 = 50% AI, 50% templates (lower = cheaper)
ELIZAOS_CACHE_TTL=300000  # Cache responses for 5 minutes (ms)
ELIZAOS_LLM_TIMEOUT=10000  # LLM timeout in ms (10s default)

# Optional: OpenAI (if you prefer over Groq)
OPENAI_API_KEY=

# Server
PORT=8765
NODE_ENV=development

# Optional: API Key for requests
API_KEY=

# ========================================
# RAILWAY DEPLOYMENT NOTES
# ========================================
#
# When deploying to Railway:
#
# 1. Required Variables:
#    - PRIVATE_KEY: Your wallet private key for Monad testnet
#    - GROQ_API_KEY: Get from https://console.groq.com/
#    - PORT: Railway auto-assigns (default 3000), but we use 8765
#    - NODE_ENV: Set to "production"
#
# 2. Build Command: bun install
# 3. Start Command: bun src/index.ts
# 4. Root Directory: backend
#
# 5. Railway will provide:
#    - RAILWAY_ENVIRONMENT: "production"
#    - RAILWAY_PROJECT_NAME: "holymon-backend"
#    - RAILWAY_SERVICE_NAME: Auto-generated
#
# 6. Important Notes:
#    - Railway doesn't support persistent storage (.eliza/ database)
#    - Backend will fall back to procedural dialogue gracefully
#    - WebSocket is supported natively on Railway
#    - No need for Dockerfile with Bun runtime
#
# 7. Verification:
#    - Test: curl https://your-app.railway.app/health
#    - WebSocket: wscat -c https://your-app.railway.app/tournament/ws
#
# 8. Frontend Integration:
#    - Set BACKEND_URL=https://your-app.railway.app in Vercel
#    - Set NEXT_PUBLIC_BACKEND_URL=https://your-app.railway.app in Vercel
#    - Redeploy frontend to pick up new environment variables
